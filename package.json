{
  "name": "mucaro-stack",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "setup": "pnpm install && pnpm db:setup && echo '\n✅ Setup complete! Starting dev server...\n' && pnpm dev",
    "db:setup": "pnpm --filter @workspace/db setup",
    "db:migrate": "pnpm --filter @workspace/db migrate",
    "db:studio": "pnpm --filter @workspace/db studio",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.2",
    "@workspace/typescript-config": "workspace:*",
    "husky": "^9.1.7",
    "turbo": "^2.5.5",
    "typescript": "5.7.3",
    "ultracite": "5.3.3"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=20"
  }
}
